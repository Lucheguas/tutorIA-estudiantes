PORT=3001
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_KEY=xxxx

# LLM — Ollama corre en la MISMA máquina que el backend, se llama por localhost
# (así se evita el 403 del edge de Cloudflare en el salto backend->LLM)
LLM_BASE_URL=http://localhost:11434
LLM_MODEL=llama3:8b
